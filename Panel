<?php

// expected apk size (bytes)
$expected_size = 25400000;  

// client se aya size
$app_size = $_POST['app_size'];
$device_id = $_POST['device_id'];

if ($app_size == $expected_size) {
    echo json_encode([
        "status" => "ok",
        "message" => "Access granted"
    ]);
} else {
    echo json_encode([
        "status" => "banned",
        "reason" => "Invalid application size",
        "device_id" => $device_id
    ]);
}

?>
